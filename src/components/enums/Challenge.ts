export enum ChallengeStatus {
  before = 'before',
  during = 'during',
  after = 'after',
}

export enum PriceLevelCategory {
  basic = 'basic',
  company = 'company',
  school = 'school',
  basicWithReward = 'basic-with-reward',
  companyWithReward = 'company-with-reward',
  schoolWithReward = 'school-with-reward',
}

export enum CompetitorType {
  team = 'team',
  singleUser = 'single_user',
}

export enum CompetitionType {
  frequency = 'frequency',
  length = 'length',
}
