export enum RegisterChallengeStep {
  personalDetails = 'personalDetails',
  payment = 'payment',
  participation = 'participation',
  organization = 'organization',
  team = 'team',
  merch = 'merch',
  summary = 'summary',
}

export enum StepVisitState {
  notVisited = 'not-visited',
  active = 'active',
  dirty = 'dirty',
}
